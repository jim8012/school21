#include <stdio.h>
char temp;

while(m < j)
	{
	j = 0;
	while( j<7)
    {
        if(foo[i]<foo[j])
        {
            temp = foo[i];
            foo[i] = foo[j];
            foo[j] = temp;
        }
      j++
    }
	i++;
	}

int		main(int argc, char *argv[])
{
	printf("&s",compare("1234", "1245");
	return (0);
}
